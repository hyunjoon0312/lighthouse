# Lighthouse 작업 지침

@.agents/common/RTK.md
@.agents/common/CODEX_RULES.md

## 사용자 기본 모델 분담

- 모든 오케스트레이션, 브레인스토밍, 계획, 아키텍처·구현 방법 결정, 복잡하거나 중요한 분석·검토·검증은 `gpt-6-astra`가 맡는다.
- 확정된 계획의 단순 코드 작성, 일반 수정, 정형 실행은 `gpt-5.6-sol`에 맡긴다. 코드라고 모두 단순 작업으로 분류하지 않는다. 구현 중 설계가 다시 필요하면 Astra가 결정하고 Sol이 반영한다.
- 복합 작업은 이 경계로 나누어 위임한다. 모델 선택이 애매하면 사용자에게 먼저 묻는다. 지정 모델을 선택할 수 없거나 가용하지 않으면 한계를 밝히고 대체 전에 묻는다.
- 실제 전환을 확인하지 않고 전환했다고 주장하지 않는다. 실질적인 하위 작업이 없는 단순 답변에는 불필요한 에이전트를 만들지 않는다. 개별 작업의 명시적 모델·effort가 우선한다.
- 블로그 본문 Astra high, 제목 추천 Sol medium 등 기존의 명시적 작업 설정은 유지한다. 지침 변경을 앱 런타임 모델 변경으로 보고하지 않는다.
- 복합 작업의 모델 분담·위임 및 관련 스킬·지침 설계 전 [.agents/skills/astra-sol-workflow/SKILL.md](.agents/skills/astra-sol-workflow/SKILL.md)를 읽는다. 판단이 핵심인 계획·정책·스킬 문서는 Astra가 직접 쓴다.
- 새 프로젝트에도 공통 규칙과 스킬의 실제 파일을 포함하고 상대 경로로 연결한다. 홈 디렉터리 링크만으로 대체하지 않는다.

## 프로젝트

macOS용 사진 정리·선별·비파괴 RAW 보정 앱. 주 사용 카메라는 Panasonic LUMIX DC-S9이며, 첫 버전의 범위와 구조는 [docs/architecture.md](docs/architecture.md)를 따른다.

- 원본 사진을 변경·삭제·덮어쓰지 않는다. 내보내기는 별도 파일로 만든다.
- 사진, 카탈로그, 보정값은 로컬에 유지한다. 외부 업로드를 추가하지 않는다.
- RAW 지원 목록 조회와 실제 RW2 디코딩 검증을 구분한다.
- UI 및 이미지 처리 코드는 각각 정해진 파일 소유권을 지킨다.
- `swift test`, `swift build`, 실제 `.app` 실행으로 관련 범위를 검증한다. 성능·색감의 검증하지 않은 수준을 보장하지 않는다.

## 장기 기억

이 환경의 공유 기억은 `/Users/joon/rian/rian-obsidian`에 있다. 이전 작업을 이어갈 때 `20 Areas/AI Context/CONTEXT.md`를, 볼트에 쓸 때 해당 볼트의 `AGENTS.md`를 읽는다. 이 경로는 선택적인 세션 기억이며 앱의 빌드·실행 의존성이 아니다. 다음 세션에 필요한 결정과 남은 검증은 세션 종료 전에 `context.py save`로 기록한다.
